{
  "AWSTemplateFormatVersion": "2010-09-09",
  "Description": "AWS CloudFormation template to create custom AWS Config rules. You will be billed for the AWS resources used if you create a stack from this template.",
  "Resources": {
    "EC2INSTANCENOPUBLICIP": {
      "Type": "AWS::CloudFormation::Stack",
      "Properties": {
        "TemplateURL": "https://config-rule-code-bucket-004670408193-us-east-1.s3.amazonaws.com/EC2_INSTANCE_NO_PUBLIC_IP/EC2_INSTANCE_NO_PUBLIC_IP-config-rule.json",
        "Parameters": {
          "LambdaAccountId": {
            "Ref": "LambdaAccountId"
          }
        }
      }
    },
    "S3BUCKETSERVERSIDEENCRYPTIONENABLED": {
      "Type": "AWS::CloudFormation::Stack",
      "Properties": {
        "TemplateURL": "https://config-rule-code-bucket-004670408193-us-east-1.s3.amazonaws.com/S3_BUCKET_SERVER_SIDE_ENCRYPTION_ENABLED/S3_BUCKET_SERVER_SIDE_ENCRYPTION_ENABLED-config-rule.json",
        "Parameters": {
          "LambdaAccountId": {
            "Ref": "LambdaAccountId"
          }
        }
      }
    }
  },
  "Conditions": {},
  "Parameters": {
    "LambdaAccountId": {
      "Description": "Account ID that contains Lambda functions for Config Rules.",
      "Type": "String",
      "MinLength": "12",
      "MaxLength": "12"
    }
  },
  "Metadata": {
    "AWS::CloudFormation::Interface": {
      "ParameterGroups": [
        {
          "Label": {
            "default": "Lambda Account ID"
          },
          "Parameters": [
            "LambdaAccountId"
          ]
        }
      ],
      "ParameterLabels": {
        "LambdaAccountId": {
          "default": "REQUIRED: Account ID that contains Lambda Function(s) that back the Rules in this template."
        }
      }
    }
  }
}